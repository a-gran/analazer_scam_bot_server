# Подключаем модуль os — он умеет читать «переменные окружения»
# (специальные настройки операционной системы, как секретные записки для программ)
import os

from dotenv import load_dotenv
load_dotenv()

# ── Токен бота ──────────────────────────────────────────────────────────────
# Способ 1 (рекомендуется): задать переменную окружения BOT_TOKEN
# Способ 2 (для теста): вставить строку напрямую: BOT_TOKEN = "123456:AAF..."

# Читаем переменную окружения BOT_TOKEN из системы.
# Если токен не задан, сразу показываем понятную ошибку при запуске.
BOT_TOKEN = os.getenv("BOT_TOKEN")

if not BOT_TOKEN:
    raise RuntimeError(
        "Не задан BOT_TOKEN. Добавь его в файл .env или в переменные окружения."
    )

# ── Игровые константы (не менять) ────────────────────────────────────────────

# Сколько жизней у игрока в начале игры — как в видеоиграх, три попытки ошибиться
LIVES = 3
# Сколько очков даётся за каждый правильный ответ
POINTS = 10
# Сколько вопросов выбирается из базы на одну игру
COUNT = 20

# Список строк — главные признаки мошенничества (скама),
# которые показываются игроку в конце игры как «шпаргалка»
SCAM_SIGNS = [
    "просят пароль или код подтверждения",
    "слишком выгодное предложение (бесплатный приз)",
    "искусственная срочность (только сейчас!)",
    "подозрительные ссылки на незнакомые сайты",
    "просят данные карты или CVV",
]
